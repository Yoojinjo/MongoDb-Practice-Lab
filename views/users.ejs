<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title>SBA Users</title>
		<!-- <link rel="stylesheet" href="../public/index.css" /> -->
		<link rel="stylesheet" href="/index.css" />
		<!-- path is because express.static("public") -->
	</head>
	<body>
		<h1>Current Users</h1>

		<nav>
			<ul>
				<li><a href="/">Home</a></li>
				<li><a class="active" href="/users"> Current Users</a></li>
				<li><a href="/posts"> Current Posts</a></li>
				<li><a href="/movies"> Current Movies</a></li>
			</ul>
		</nav>
		<!-- Dynamically render the list of movies -->
		<ol>
			<% if (users.length > 0) { %> <% users.forEach(user => { %>
			<li class="user-data">
				<h2><%= user.name %></h2>
				<p><strong>email:</strong> <%= user.email%></p>
				<p><strong>id:</strong> <%= user._id%></p>
			</li>

			<% }) } else { %>
			<li>No movies available</li>
			<% } %>
		</ol>
	</body>
</html>
